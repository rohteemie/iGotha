sudo service mysql stop

sudo mysqld_safe --skip-grant-tables &

# while in mysqld_safe
mysql -u root

SELECT CONCAT('*', UPPER(SHA1(UNHEX(SHA1('Your Password here')))));

USE mysql;
UPDATE user SET authentication_string='*HASHED_PASSWORD' WHERE User='root' AND Host='localhost';

ALTER USER 'root'@'localhost' IDENTIFIED BY 'new_non_hashed_password';

FLUSH PRIVILEGES;

EXIT;

sudo killall mysqld

sudo service mysql start

sudo mysql -uroot -p(password-not-recommended)


---------------------------

SELECT User, Host FROM mysql.user;
